<!--
Thanks for contributing! One thing before you submit:

⚠️ Please target the `beta` branch — that's where development happens.
   `main` only receives release merges.

If you accidentally opened this against `main`, just change the base branch
with the "Edit" button at the top of the PR — no need to redo any work.
-->

## What does this change?

<!-- A short description. If it addresses an issue, link it (e.g. "Fixes #4"). -->

## Checklist

- [ ] Targets the `beta` branch
- [ ] `swift build` and `swift test` pass locally
- [ ] Follows the existing code style (see CONTRIBUTING.md)
